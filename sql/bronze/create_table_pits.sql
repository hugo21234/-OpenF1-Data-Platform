CREATE TABLE pits (
    date STRING,
    driver_number BIGINT,
    lane_duration DOUBLE,
    lap_number BIGINT,
    meeting_key INT,
    session_key INT,
    pit_duration DOUBLE,
    stop_duration DOUBLE
)
