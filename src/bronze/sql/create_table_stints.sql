CREATE TABLE stints (
    compound STRING,
    driver_number BIGINT,
    lap_end BIGINT,
    lap_start BIGINT,
    meeting_key BIGINT,
    session_key BIGINT,
    stint_number BIGINT,
    tyre_age_at_start BIGINT
)
